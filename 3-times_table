#include <stdio.h>

#include <stdlib.h>

int main()
{
int number;

int i;

for(i = 1, number = 3; i<= 20; i++)
{
int answer = number * i;
char operator = '*';

printf("%d * %d = %d\n", number, i, answer);
}
return 0;
}
